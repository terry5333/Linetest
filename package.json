{
  "name": "terryedu-line-menu-test",
  "version": "1.0.0",
  "main": "api/webhook.ts",
  "scripts": {
    "build": "mkdir -p public && echo '<html><body>API is running</body></html>' > public/index.html"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": "20.x"
  }
}
